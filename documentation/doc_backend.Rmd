---
title: "Biotablero Assessent"
output: html_notebook
---
El objetivo de este trabajo es analizar cuales de las salidas gráficas que produce biotablero son compatibles con las de Backend sin necesidad de agregar datos nuevos.  
Evaluación de las salidas de Biotablero y el Backenbd:

1. Envíar pérdida de Bosque. Done. Reproyectado y alienado con lo demás 

2. Nuevas salidas de datos que puedan representarse más fácil en Biotablero

3. Consultas perosnalizadas.Que tipo de consultas se pueden hacer?

4. Biotablero tiene áreas predefinidas, pero nosotros queremos areas custom 

5. Que indicadores se peuden usar para analizar esa información?


## Salidas de datos.



### Ecosistemas

1. Bosques
  Hansen 2000-2018 (actualizar a  2021) por que carajos temriona en 2012 la gráfica?
  IDEAM (1990-2016) (agregar los nuevos que tenemos)
2. Corine Land Cover
3. Lista Roja Ecosistemas
4. Región Biótica 
5. Biomas
6. Bosque Seco Tropical
7. Páramos
8. Humedales

### Manejo
1. Areas Protegidas
2. Areas Colectivas
3. Manejo Especial
4. Factor de Compensación

### Especies
1. UICN 
2. Biomodelos
3. GBIF

### Índices

1. Superficies y Registro 
Area
### 


System Files:

ubuntu@ip-172-31-7-208:~$ 

1. Armonized_msk. Permission denied (possibly it was me who uplodade it there, and nothing else happened)
2. dbds: Databases: N1_2000_2002.tif.vat.dbf. don't know what it stands for
  biome.tif.vat.dbf 
  bioticreg.tif.vat.dbf
  colectrareas.tif.vat.dbf
  ecouicn.tif.vat.dbf
  faccomp.tif.vat.dbf. Don't know what it is
  hum.tif.vat.dbf
  param.tif.vat.dbf
  protectareas.tif.vat.dbf
  sma.tif.vat.dbf
  tropdryforest.tif.vat.dbf

3. others: ecochange, test polygons
4. Plumber. The importat setup and pricessing scripts and routines are here 
5. snap: Emacs Good to know. I don't remember if it was me who put it here or if it already was here. 
6. ssd. Some messages and images for the shiny applciation . Not too important, even less for running anything.
7. SAhiny server deb. Don't know waht it does and how it works.


Root directory: ubuntu@ip-172-31-7-208:/data$: downloaded data fron Ivan's cloud storage. Have not been able to know how to put data there/or if i need to change the source.

temR: temporary files. Don't know if we have to delete them 
cd rli
cd 


En esteThis is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r}
plot(cars)
```

Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Cmd+Option+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Cmd+Shift+K* to preview the HTML file). 

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.

